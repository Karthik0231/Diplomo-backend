# 1. create one folder inside that folder run "npm init -y"
# 2. after executing that cmd you will get package.json there you have to change "type":"module"
# 3. create one entryFile (anyName.js)
# 4. install express package (npm install express)
# 5. basic setup in index.js
# 6. to run use -> "node entryFile.js" or "nodemon entryFile.js"
# 7. create db.js for db connection and call it in entryfile
# 8. create 3 folders models, controller, routes
#  models -> manage schemas, controller -> business logic, routes-> manage routing 
